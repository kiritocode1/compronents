"use client";

import ClipMaskTransitionPage from "@/registry/clip-mask-transition-page";

const BASE = "/assets/clip-mask-transition-page";

export default function ClipMaskTransitionPageDemo() {
  return <ClipMaskTransitionPage assetBase={BASE} />;
}
