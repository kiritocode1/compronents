"use client";

import SlidingIndexMenu from "@/registry/sliding-index-menu";

export default function SlidingIndexMenuDemo() {
  return (
    <div className="relative h-[100svh] w-full overflow-hidden bg-black">
      <SlidingIndexMenu />
    </div>
  );
}
