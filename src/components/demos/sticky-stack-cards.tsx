"use client";

import StickyStackCards from "@/registry/sticky-stack-cards";

export default function StickyStackCardsDemo() {
  return (
    <div className="relative h-[100svh] w-full overflow-hidden bg-black">
      <StickyStackCards />
    </div>
  );
}
