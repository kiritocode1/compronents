"use client";

import ScrollScrubSlider from "@/registry/scroll-scrub-slider";

export default function ScrollScrubSliderDemo() {
  return (
    <div className="relative h-[100svh] w-full overflow-hidden bg-[#0f0f0f]">
      <ScrollScrubSlider />
    </div>
  );
}
