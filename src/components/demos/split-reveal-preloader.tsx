"use client";

import SplitRevealPreloader from "@/registry/split-reveal-preloader";

export default function SplitRevealPreloaderDemo() {
  return (
    <div className="relative h-[100svh] w-full overflow-hidden bg-[#0a0a0a]">
      <SplitRevealPreloader />
    </div>
  );
}
