"use client";

import RevealerTransitionPage from "@/registry/revealer-transition-page";

const BASE = "/assets/revealer-transition-page";

export default function RevealerTransitionPageDemo() {
  return <RevealerTransitionPage assetBase={BASE} />;
}
