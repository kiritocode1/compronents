"use client";

import WrappedDragIndex from "@/registry/wrapped-drag-index";

export default function WrappedDragIndexDemo() {
  return (
    <div className="relative h-[100svh] w-full overflow-hidden bg-black">
      <WrappedDragIndex />
    </div>
  );
}
