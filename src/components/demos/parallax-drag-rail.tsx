"use client";

import ParallaxDragRail from "@/registry/parallax-drag-rail";

export default function ParallaxDragRailDemo() {
  return (
    <div className="relative h-[100svh] w-full overflow-hidden bg-[#0f0f0f]">
      <ParallaxDragRail />
    </div>
  );
}
