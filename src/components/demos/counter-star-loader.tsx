"use client";

import CounterStarLoader from "@/registry/counter-star-loader";

export default function CounterStarLoaderDemo() {
  return (
    <div className="relative h-[100svh] w-full overflow-hidden bg-black">
      <CounterStarLoader />
    </div>
  );
}
