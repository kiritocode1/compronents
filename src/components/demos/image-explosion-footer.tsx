"use client";

import ImageExplosionFooter from "@/registry/image-explosion-footer";

export default function ImageExplosionFooterDemo() {
  return (
    <div className="relative h-[100svh] w-full overflow-hidden bg-[#0f0f0f]">
      <ImageExplosionFooter />
    </div>
  );
}
