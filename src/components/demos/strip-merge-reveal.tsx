"use client";

import StripMergeReveal from "@/registry/strip-merge-reveal";

export default function StripMergeRevealDemo() {
  return (
    <div className="relative h-[100svh] w-full overflow-hidden bg-[#0f0f0f]">
      <StripMergeReveal />
    </div>
  );
}
